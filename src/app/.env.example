# Leave both empty to run Rota Doc with the local browser account.
# Fill them with a Supabase project to get real sign-in and a route that
# follows the account across browsers. Both are public client keys.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
